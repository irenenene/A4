#include "Student.h"
#include "List.h"
#include "Queue.h"

#include <iostream>
#include <fstream>

using namespace std;

int main (int argc, char **argv) {
  /*Queue<Student> *intQueue;
  intQueue = new Queue<Student>();
  Student bob(10);
  Student alice(20);

  intQueue->insert(bob);
  intQueue->insert(alice);
  intQueue->printList();
  cout << *intQueue->peek();
  delete intQueue;*/
}
